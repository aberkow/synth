var duoSynth = require('./duoSynth');

var voiceWaveConfig = function(data, voiceWave){
  voiceWave = data.text;
}

// var voiceWaveConfig = function(data, index){
//   duoSynth.voice + index + . + oscillator.type = data.text;
// }

module.exports = voiceWaveConfig;
